#include "sbnToyFragment.hh"

#include "cetlib_except/exception.h"

/*std::ostream& sbndaq::operator<<(std::ostream& os, sbnToyFragment const& f)
{
  os << "sbnToyFragment fill time: " << f.hdr_event_size() << ", trigger number: " << f.hdr_trigger_number() << "\n";

  return os;
} */
